## 2024-06-03 - [Optimize MultiheadAttention with need_weights=False]
**Learning:** PyTorch's `nn.MultiheadAttention` calculates and returns attention weights by default, which can cause unnecessary computation and memory allocation if these weights are simply discarded (e.g., into `_`). However, `need_weights=False` should only be used when the returned attention weights are discarded into `_` and not explicitly unpacked into named variables, because unpacking `None` could break downstream usage.
**Action:** When using `nn.MultiheadAttention`, use `need_weights=False` if the attention weights are not needed and discarded to `_`. Double check before adding it if weights are actually used or unpacked into a named variable (e.g. `cross_attn_weights`).
* Many tests have pre-existing failures or import errors causing global collection to fail (e.g., test_disease_biomarkers.py). To run the test suite cleanly, extensively exclude them using `--ignore` during `pytest` runs (e.g., `PYTHONPATH=src pytest tests/ --ignore=tests/test_disease_biomarkers.py`), or alternatively, run specific isolated test files (e.g., `PYTHONPATH=src python -m pytest tests/test_har_baseline.py`).
